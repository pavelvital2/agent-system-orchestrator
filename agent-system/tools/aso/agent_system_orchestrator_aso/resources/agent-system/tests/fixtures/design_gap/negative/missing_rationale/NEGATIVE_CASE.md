# Negative case: missing recommendation rationale

Expected failure: `DG4_QUESTION_006`.

The question card includes a recommendation but omits the plain-language reason.
Corrected P4 requires recommendation rationale so owner-facing guidance can be
audited.
